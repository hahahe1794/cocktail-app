services:
  cocktail-app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - cocktail-data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000

volumes:
  cocktail-data:
